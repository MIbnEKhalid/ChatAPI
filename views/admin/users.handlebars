<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management | MBK_AI</title>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --bg: #050505; 
            --panel: #0a0a0a; 
            --border: #333;
            --primary: #00ff41; 
            --text: #e0e0e0; 
            --dim: #666;
            --danger: #ff003c;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-track { background: var(--bg); }
        ::-webkit-scrollbar-thumb { background: #333; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: var(--primary); }

        * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'JetBrains Mono', monospace; }
        body { background: var(--bg); color: var(--text); display: flex; height: 100vh; overflow: hidden; }
        
        /* Sidebar */
        .sidebar { width: 250px; background: var(--panel); border-right: 1px solid var(--border); padding: 20px; display: flex; flex-direction: column; flex-shrink: 0; transition: transform 0.3s; }
        .main { flex: 1; padding: 30px; overflow-y: auto; width: 100%; position: relative; }
        
        /* Nav */
        .brand { color: var(--primary); font-size: 1.2rem; font-weight: bold; margin-bottom: 40px; display: flex; align-items: center; gap: 10px; }
        .nav-link { display: block; padding: 12px; color: var(--dim); text-decoration: none; border-radius: 4px; margin-bottom: 5px; transition: 0.2s; border: 1px solid transparent; }
        .nav-link:hover, .nav-link.active { background: rgba(0, 255, 65, 0.05); color: var(--primary); border-color: rgba(0, 255, 65, 0.2); }
        .nav-link i { width: 24px; }

        /* Search Toolbar */
        .toolbar { display: flex; gap: 10px; margin-bottom: 25px; align-items: center; background: var(--panel); padding: 15px; border: 1px solid var(--border); border-radius: 6px; }
        .search-input { background: #111; border: 1px solid var(--border); color: #fff; padding: 10px 15px; border-radius: 4px; width: 300px; outline: none; transition: border 0.3s; }
        .search-input:focus { border-color: var(--primary); }
        
        .btn { background: #222; color: #fff; border: 1px solid var(--border); padding: 10px 20px; border-radius: 4px; cursor: pointer; transition: 0.2s; font-size: 0.9rem; text-decoration: none; display: inline-flex; align-items: center; gap: 8px; }
        .btn:hover { background: #333; border-color: #555; }
        .btn-primary { background: rgba(0, 255, 65, 0.1); color: var(--primary); border-color: rgba(0, 255, 65, 0.3); font-weight: bold; }
        .btn-primary:hover { background: rgba(0, 255, 65, 0.2); box-shadow: 0 0 10px rgba(0, 255, 65, 0.1); }

        /* Table */
        .table-container { background: var(--panel); border: 1px solid var(--border); border-radius: 6px; overflow: hidden; box-shadow: 0 4px 6px rgba(0,0,0,0.3); }
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 15px 25px; text-align: left; border-bottom: 1px solid var(--border); }
        th { color: var(--dim); font-size: 0.75rem; text-transform: uppercase; font-weight: bold; letter-spacing: 1px; background: #0f0f0f; }
        tr:hover { background: rgba(255,255,255,0.02); }
        
        .user-cell { display: flex; align-items: center; gap: 10px; }
        .status-badge { font-size: 0.7rem; padding: 2px 8px; border-radius: 10px; border: 1px solid; }
        .status-active { color: var(--primary); border-color: rgba(0,255,65,0.3); background: rgba(0,255,65,0.05); }

        /* Pagination */
        .pagination { margin-top: 20px; display: flex; justify-content: space-between; align-items: center; padding-top: 20px; border-top: 1px solid var(--border); }
        .page-info { color: var(--dim); font-size: 0.9rem; }
        .page-controls { display: flex; gap: 8px; }

        /* Mobile */
        @media (max-width: 768px) {
            .sidebar { display: none; }
            .toolbar { flex-direction: column; align-items: stretch; }
            .search-input { width: 100%; }
        }
    </style>
</head>
<body>

    <nav class="sidebar">
        <div class="brand"><i class="fas fa-terminal"></i> MBK_ADMIN</div>
        <a href="/admin/dashboard" class="nav-link"><i class="fas fa-chart-line"></i> Dashboard</a>
        <a href="/admin/users" class="nav-link active"><i class="fas fa-users"></i> Users</a>
        <a href="/admin/chats" class="nav-link"><i class="fas fa-comments"></i> Chat Logs</a>
        
        <div style="flex:1"></div>
        
        <div style="padding: 12px; border-top: 1px solid #222; margin-top: auto; background: rgba(0, 255, 65, 0.02); border-radius: 4px; border-left: 3px solid var(--primary);">
            <div style="font-size: 0.7rem; color: var(--dim); text-transform: uppercase; letter-spacing: 1px; margin-bottom: 6px;">
            <i class="fas fa-circle" style="color: var(--primary); font-size: 0.5rem; margin-right: 6px;"></i> Active User
            </div>
            <div style="display: flex; align-items: center; gap: 8px; margin-bottom: 8px;">
            <i class="fas fa-user-shield" style="color: var(--primary); font-size: 1.2rem;"></i>
            <strong style="color: var(--primary); font-size: 0.9rem;">{{currentUser}}</strong>
            </div>
        </div>
        <a href="/dashboard" class="nav-link" style="margin-top: 10px;"><i class="fas fa-sign-out-alt"></i> Exit Admin</a>
    </nav>

    <main class="main">
        <h2 style="margin-bottom: 20px; display: flex; align-items: center; gap: 10px;">
            <i class="fas fa-users-cog" style="color: var(--dim);"></i> USER_DATABASE
        </h2>

        <form class="toolbar" method="GET">
            <div style="position: relative; flex:1; max-width: 400px;">
                <i class="fas fa-search" style="position: absolute; left: 15px; top: 13px; color: var(--dim);"></i>
                <input type="text" name="search" class="search-input" style="padding-left: 40px; width: 100%;" placeholder="Search by username..." value="{{searchQuery}}">
            </div>
            <button type="submit" class="btn btn-primary">SEARCH</button>
            {{#if searchQuery}}
            <a href="/admin/users" class="btn">CLEAR</a>
            {{/if}}
        </form>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Username</th>
                        <th>Total Chats</th>
                        <th>Last Active</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each users}}
                    <tr>
                        <td>
                            <div class="user-cell">
                                <i class="fas fa-user-circle" style="color: var(--dim); font-size: 1.2rem;"></i>
                                <strong>{{this.username}}</strong>
                            </div>
                        </td>
                        <td>{{this.total_chats}}</td>
                        <td style="color: var(--dim); font-size: 0.9rem; font-family: monospace;">{{this.last_active}}</td>
                        <td><span class="status-badge status-active">ACTIVE</span></td>
                    </tr>
                    {{else}}
                    <tr>
                        <td colspan="4" style="text-align: center; padding: 40px; color: var(--dim);">
                            <i class="fas fa-search" style="font-size: 2rem; margin-bottom: 10px; display:block; opacity: 0.5;"></i>
                            No users found matching "{{searchQuery}}"
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        <div class="pagination">
            <div class="page-info">
                Page <span style="color:#fff; font-weight:bold;">{{pagination.currentPage}}</span> of {{pagination.totalPages}}
            </div>
            <div class="page-controls">
                {{#if pagination.hasPrev}}
                    <a href="?page={{pagination.prevPage}}&search={{searchQuery}}" class="btn"><i class="fas fa-chevron-left"></i> Prev</a>
                {{else}}
                    <span class="btn" style="opacity: 0.3; cursor: default;">Prev</span>
                {{/if}}

                {{#if pagination.hasNext}}
                    <a href="?page={{pagination.nextPage}}&search={{searchQuery}}" class="btn">Next <i class="fas fa-chevron-right"></i></a>
                {{else}}
                    <span class="btn" style="opacity: 0.3; cursor: default;">Next</span>
                {{/if}}
            </div>
        </div>
    </main>
</body>
</html>