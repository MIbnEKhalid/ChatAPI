<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management | MBK_AI</title>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root { --bg: #050505; --panel: #0a0a0a; --border: #333; --primary: #00ff41; --text: #e0e0e0; --dim: #666; }
        * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'JetBrains Mono', monospace; }
        body { background: var(--bg); color: var(--text); display: flex; min-height: 100vh; }
        
        .sidebar { width: 250px; background: var(--panel); border-right: 1px solid var(--border); padding: 20px; display: flex; flex-direction: column; }
        .main { flex: 1; padding: 30px; overflow-y: auto; }
        .brand { color: var(--primary); font-size: 1.2rem; font-weight: bold; margin-bottom: 40px; }
        .nav-link { display: block; padding: 12px; color: var(--dim); text-decoration: none; border-radius: 4px; margin-bottom: 5px; }
        .nav-link:hover, .nav-link.active { background: rgba(0, 255, 65, 0.1); color: var(--primary); }
        .nav-link i { width: 24px; }

        /* Search Bar */
        .toolbar { display: flex; gap: 10px; margin-bottom: 20px; }
        .search-input { background: #111; border: 1px solid var(--border); color: #fff; padding: 10px; border-radius: 4px; width: 300px; }
        .btn { background: #222; color: #fff; border: 1px solid var(--border); padding: 10px 20px; border-radius: 4px; cursor: pointer; }
        .btn-primary { background: var(--primary); color: #000; border: none; font-weight: bold; }

        .table-container { background: var(--panel); border: 1px solid var(--border); border-radius: 6px; }
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 15px 20px; text-align: left; border-bottom: 1px solid var(--border); }
        th { color: var(--dim); font-size: 0.8rem; text-transform: uppercase; font-weight: normal; }
        tr:hover { background: rgba(255,255,255,0.02); }

        .pagination { margin-top: 20px; display: flex; gap: 5px; justify-content: flex-end; }
        .page-link { padding: 8px 12px; border: 1px solid var(--border); color: var(--dim); text-decoration: none; border-radius: 4px; }
        .page-link:hover { color: #fff; border-color: #fff; }
    </style>
</head>
<body>

    <nav class="sidebar">
        <div class="brand">MBK_ADMIN</div>
        <a href="/admin/dashboard" class="nav-link"><i class="fas fa-chart-line"></i> Dashboard</a>
        <a href="/admin/users" class="nav-link active"><i class="fas fa-users"></i> Users</a>
        <a href="/admin/chats" class="nav-link"><i class="fas fa-comments"></i> Chat Logs</a>
    </nav>

    <main class="main">
        <h2 style="margin-bottom: 20px;">USER_DATABASE</h2>

        <form class="toolbar" method="GET">
            <input type="text" name="search" class="search-input" placeholder="Search username..." value="{{searchQuery}}">
            <button type="submit" class="btn btn-primary">SEARCH</button>
        </form>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th>Username</th>
                        <th>Total Msgs</th>
                        <th>Last Active</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each users}}
                    <tr>
                        <td><strong>{{this.username}}</strong></td>
                        <td>{{this.total_messages}}</td>
                        <td>{{this.last_active}}</td>
                        <td><span style="color:var(--primary)">ACTIVE</span></td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        <div class="pagination">
            <a href="?page={{pagination.currentPage}}&search={{searchQuery}}" class="page-link">Prev</a>
            <span class="page-link" style="border:none; color:#fff;">Page {{pagination.currentPage}} / {{pagination.totalPages}}</span>
            <a href="?page={{pagination.currentPage}}&search={{searchQuery}}" class="page-lin