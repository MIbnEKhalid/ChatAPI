<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard | MBK_AI</title>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --bg: #050505; 
            --panel: #0a0a0a; 
            --border: #333;
            --primary: #00ff41; 
            --text: #e0e0e0; 
            --dim: #666;
            --danger: #ff003c;
            --glass-red: rgba(255, 0, 60, 0.15);
            --glass-red-border: rgba(255, 0, 60, 0.4);
        }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-track { background: var(--bg); }
        ::-webkit-scrollbar-thumb { background: #333; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: var(--primary); }

        * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'JetBrains Mono', monospace; }
        body { background: var(--bg); color: var(--text); display: flex; height: 100vh; overflow: hidden; }
        
        /* Sidebar */
        .sidebar { width: 250px; background: var(--panel); border-right: 1px solid var(--border); padding: 20px; display: flex; flex-direction: column; flex-shrink: 0; transition: transform 0.3s; }
        .main { flex: 1; padding: 30px; overflow-y: auto; width: 100%; position: relative; }
        
        .brand { color: var(--primary); font-size: 1.2rem; font-weight: bold; margin-bottom: 40px; display: flex; align-items: center; gap: 10px; }
        .nav-link { display: block; padding: 12px; color: var(--dim); text-decoration: none; border-radius: 4px; margin-bottom: 5px; transition: 0.2s; border: 1px solid transparent; }
        .nav-link:hover, .nav-link.active { background: rgba(0, 255, 65, 0.05); color: var(--primary); border-color: rgba(0, 255, 65, 0.2); }
        .nav-link i { width: 24px; }
        
        /* Stats Grid - CHANGED to 3 columns since Active Today was removed */
        .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-bottom: 30px; }
        
        .card { background: var(--panel); border: 1px solid var(--border); padding: 25px; border-radius: 6px; position: relative; overflow: hidden; transition: transform 0.2s; }
        .card:hover { transform: translateY(-2px); border-color: #444; }
        .card::after { content:''; position: absolute; top:0; left:0; width: 4px; height: 100%; background: var(--primary); opacity: 0.5; }
        .card.danger::after { background: var(--danger); }
        
        .card-icon { position: absolute; top: 20px; right: 20px; font-size: 1.5rem; color: #222; }
        .stat-value { font-size: 2.2rem; color: #fff; font-weight: bold; margin: 10px 0; }
        .stat-label { color: var(--dim); font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px; font-weight: bold; }
        
        /* Chart Section */
        .chart-section { margin-bottom: 30px; background: var(--panel); border: 1px solid var(--border); border-radius: 6px; padding: 20px; }
        .chart-header { display: flex; justify-content: space-between; margin-bottom: 15px; align-items: center; }
        .chart-box { height: 200px; display: flex; align-items: flex-end; gap: 4px; padding-top: 20px; border-bottom: 1px solid #222; }
        
        .bar-group { flex: 1; display: flex; flex-direction: column; justify-content: flex-end; height: 100%; position: relative; cursor: pointer; }
        .bar { width: 100%; background: var(--primary); opacity: 0.4; min-height: 2px; transition: all 0.3s; border-radius: 2px 2px 0 0; }
        .bar-group:hover .bar { opacity: 1; background: #fff; box-shadow: 0 0 10px rgba(0,255,65,0.5); }
        
        .bar-label { font-size: 10px; color: #444; text-align: center; margin-top: 5px; display: none; }
        .bar-group:nth-child(4n+1) .bar-label { display: block; } 
        
        /* Table Section */
        .table-section { background: var(--panel); border: 1px solid var(--border); border-radius: 6px; overflow: hidden; box-shadow: 0 4px 6px rgba(0,0,0,0.3); }
        .table-header { padding: 15px 25px; border-bottom: 1px solid var(--border); font-weight: bold; color: var(--primary); letter-spacing: 1px; background: #0f0f0f; }
        .table-responsive { overflow-x: auto; }
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 15px 25px; text-align: left; border-bottom: 1px solid var(--border); white-space: nowrap; }
        th { color: var(--dim); font-size: 0.75rem; text-transform: uppercase; font-weight: normal; }
        
        /* Table Rows */
        tr { transition: 0.2s; border-left: 3px solid transparent; }
        tr:hover { background: rgba(255,255,255,0.02); }
        
        /* Deleted Row Style */
        .row-deleted {
            background: var(--glass-red) !important;
            border-left: 3px solid var(--danger) !important;
        }
        .row-deleted td { color: #ffcccc; border-bottom: 1px solid var(--glass-red-border); }
        .row-deleted .badge-deleted { border-color: rgba(255,255,255,0.5); }

        /* Badges */
        .badge { padding: 3px 8px; border-radius: 4px; font-size: 0.75rem; font-weight: bold; text-transform: uppercase; display: inline-block; }
        .badge-active { background: rgba(0, 255, 65, 0.1); color: var(--primary); border: 1px solid rgba(0, 255, 65, 0.2); }
        .badge-deleted { background: rgba(255, 0, 60, 0.2); color: #fff; border: 1px solid var(--danger); }

        /* Mobile responsive */
        @media (max-width: 1200px) {
            .grid { grid-template-columns: repeat(2, 1fr); }
        }
        @media (max-width: 768px) {
            .grid { grid-template-columns: 1fr; }
            .sidebar { display: none; }
        }
    </style>
</head>
<body>

    <nav class="sidebar">
        <div class="brand"><i class="fas fa-terminal"></i> MBK_ADMIN</div>
        <a href="/admin/dashboard" class="nav-link active"><i class="fas fa-chart-line"></i> Dashboard</a>
        <a href="/admin/users" class="nav-link"><i class="fas fa-users"></i> Users</a>
        <a href="/admin/chats" class="nav-link"><i class="fas fa-comments"></i> Chat Logs</a>
        
        <div style="flex:1"></div>
        
        <div style="padding: 12px; border-top: 1px solid #222; margin-top: auto; background: rgba(0, 255, 65, 0.02); border-radius: 4px; border-left: 3px solid var(--primary);">
            <div style="font-size: 0.7rem; color: var(--dim); text-transform: uppercase; letter-spacing: 1px; margin-bottom: 6px;">
            <i class="fas fa-circle" style="color: var(--primary); font-size: 0.5rem; margin-right: 6px;"></i> Active User
            </div>
            <div style="display: flex; align-items: center; gap: 8px; margin-bottom: 8px;">
            <i class="fas fa-user-shield" style="color: var(--primary); font-size: 1.2rem;"></i>
            <strong style="color: var(--primary); font-size: 0.9rem;">{{currentUser}}</strong>
            </div>
            <div style="font-size: 0.65rem; color: #666;">Admin Access Level</div>
        </div>
        
        <a href="/admin" class="nav-link" style="margin-top: 10px;"><i class="fas fa-sign-out-alt"></i> Exit Admin</a>
    </nav>

    <main class="main">
        <h2 style="margin-bottom: 30px; display: flex; align-items: center; gap: 10px;">
            <i class="fas fa-tachometer-alt" style="color:var(--dim);"></i> SYSTEM_OVERVIEW
        </h2>
        
        <!-- UPDATED GRID: Removed 'Active Today' card -->
        <div class="grid">
            <div class="card">
                <i class="fas fa-comments card-icon"></i>
                <div class="stat-label">Total Conversations</div>
                <div class="stat-value">{{stats.total_chats}}</div>
            </div>
            <div class="card">
                <i class="fas fa-users card-icon"></i>
                <div class="stat-label">Unique Users</div>
                <div class="stat-value">{{stats.unique_users}}</div>
            </div>
            <div class="card danger">
                <i class="fas fa-trash card-icon"></i>
                <div class="stat-label">Deleted Logs</div>
                <div class="stat-value" style="color:var(--danger)">{{stats.deleted_chats}}</div>
            </div>
        </div>

        <div class="chart-section">
            <div class="chart-header">
                <div class="stat-label">Request Volume (24 Hours)</div>
                <div style="font-size: 0.8rem; color: var(--dim);">UTC Time</div>
            </div>
            <div class="chart-box" id="chart">
                <!-- Javascript will populate this -->
            </div>
        </div>

        <div class="table-section">
            <div class="table-header"><i class="fas fa-history"></i> RECENT ACTIVITY (Last 10)</div>
            <div class="table-responsive">
                <table>
                    <thead>
                        <tr>
                            <th>User</th>
                            <th>Timestamp</th>
                            <th>Msgs</th>
                            <th>Status</th>
                            <th>Reference ID</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each recentChats}}
                        <tr class="{{#if this.is_deleted}}row-deleted{{/if}}">
                            <td>
                                <div style="display:flex; align-items:center; gap:8px;">
                                    <i class="fas fa-user-circle" style="opacity:0.5"></i>
                                    <strong>{{this.username}}</strong>
                                </div>
                            </td>
                            <td style="color: var(--dim); font-size: 0.9rem;">{{this.created_at}}</td>
                            <td><span style="background:#222; padding:2px 6px; border-radius:4px; font-size:0.8rem; color: #fff;">{{this.message_count}}</span></td>
                            <td>
                                {{#if this.is_deleted}}
                                    <span class="badge badge-deleted">DELETED</span>
                                {{else}}
                                    <span class="badge badge-active">ACTIVE</span>
                                {{/if}}
                            </td>
                            <td style="font-family:monospace; color:var(--dim); font-size: 0.85rem;">{{this.id}}</td>
                        </tr>
                        {{else}}
                        <tr>
                            <td colspan="5" style="text-align: center; padding: 40px; color: var(--dim);">
                                <i class="fas fa-inbox" style="font-size: 2rem; margin-bottom: 10px; display:block;"></i>
                                No recent activity found.
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>

    </main>

    <script>
        // Use try-catch to prevent page crash if handlebars variable is empty or malformed
        let data = [];
        try {
            const rawData = '{{hourlyData}}'; 
            if (rawData && rawData !== 'undefined') {
                data = JSON.parse(rawData);
            } else {
                console.warn('No chart data provided');
                data = new Array(24).fill(0);
            }
        } catch (e) {
            console.error('Error parsing chart data:', e);
            data = new Array(24).fill(0);
        }

        const chart = document.getElementById('chart');
        
        // Calculate max for scale, defaulting to 10 if all data is 0
        const max = Math.max(...data, 10); 
        
        data.forEach((val, hour) => {
            // Calculate percentage
            const h = (val / max) * 100;
            
            const group = document.createElement('div');
            group.className = 'bar-group';
            
            const bar = document.createElement('div');
            bar.className = 'bar';
            // If value is 0, show a 1px line so the timeline is visible
            bar.style.height = val > 0 ? `${h}%` : '1px';
            bar.title = `${hour}:00 - ${val} messages`;
            
            // Highlight bars that have significant traffic
            if (h > 80) {
                bar.style.backgroundColor = '#fff';
            }

            const label = document.createElement('div');
            label.className = 'bar-label';
            label.innerText = hour + 'h';

            group.appendChild(bar);
            group.appendChild(label);
            chart.appendChild(group);
        });
    </script>
</body>
</html>